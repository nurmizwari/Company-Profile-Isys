import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E64936",
    },
  },
  typography: {
    fontFamily: "Poppins",
  },
});

export default theme;
